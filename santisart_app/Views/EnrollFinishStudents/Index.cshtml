@model IEnumerable<santisart_app.Models.EnrollFinishStudent>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>

@foreach (var item in Model)
{
    <table class="table">
        <tr>
            <th>
                ชื่อ - นามสกุล
            </th>

            <th>
                @Html.DisplayNameFor(model => model.Status)
            </th>
            
            <th>
                @Html.DisplayNameFor(model => model.Status)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FinishCouse)
            </th>
            <th>
                @*@Html.DisplayNameFor(model => model.FinishTypes)*@
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Staff)
            </th>

            <th>
                @Html.DisplayNameFor(model => model.TimeStam)
            </th>

            <th></th>
        </tr>

        @foreach (var items in Model)
        {
            <tr>
                <td>
                <a href=" @Url.Action("IndexById", "EnrollStudentCouse", new { id = item.StudentId })">@(item.students.Student_title + " " + item.students.Student_name + " " + item.students.Student_lname)</a>
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Status)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Student)
                </td>

                <td>
                    @Html.DisplayFor(modelItem => item.FinishType.FormName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Staff)
                </td>

                <td>
                    @Html.DisplayFor(modelItem => item.TimeStam)
                </td>

                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.FinishId }) |
                    @Html.ActionLink("Details", "Details", new { id = item.FinishId }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.FinishId })
                </td>
            </tr>
        }

    </table>
}

